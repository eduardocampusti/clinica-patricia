# 0.1.0 — consolidação local para revisão

**Estado:** integração em PRs de rascunho em 26/09/2026; nenhum merge, release ou publicação. A árvore original e seu localhost habitual permaneceram intocados.

O inventário abaixo registra a situação anterior à integração. A revisão posterior produziu a PR #2 (`codex/integracao-010-base` → `main`) para os 37 commits do checkpoint e a PR #3 (`codex/integracao-010-pacientes` → branch da PR #1) para a evolução posterior de Pacientes. A PR #1 segue em rascunho e não teve a base modificada. A branch de Sistema/versionamento depende dessas etapas; nenhuma delas ativa o workflow em `main` antes do merge revisado.

**Correções pré-merge:** a PR #2 recebeu um commit corretivo que restaura, byte a byte, os três arquivos históricos SQL ao checkpoint `d550211`; os avisos de linhas vazias ao executar `git diff --check` nesse commit refletem os bytes preexistentes, não SQL novo. A PR #3 recebeu um commit que move a proposta `20260925130000` para `supabase/review/`, fora da fila executável. As migrations de responsável e foto permanecem intactas. A PR #4 incorpora as correções da PR #3 por ancestralidade e adiciona a preparação automática das notas e testes portáveis. A PR #2 foi incorporada somente na worktree temporária de combinação; a branch #4 isolada não a contém. Nenhum merge remoto foi executado.

## Bases verificadas

| Camada | Estado confirmado | Implicação |
| --- | --- | --- |
| `origin/main` | `5fb56137e88af6a6144f6a7ce8c64592034c2855`, branch padrão na API pública; pacote remoto ainda `0.0.0` | Destino recomendado de lançamento, depois de consolidar. |
| `main` local | `851a36352dadb9dd7c0729ac7288f556a9fdc0d0`, um commit à frente de `origin/main` e ancestral do checkpoint | Não tomar o `main` local como publicado. |
| Checkpoint remoto | `d550211886ad93ad0c58d49f540aac8f81a19536`, 37 commits/228 caminhos à frente de `origin/main` | Conteúdo amplo de Financeiro, banco, segurança, shell e documentação; revisão própria antes de integrar. |
| PR #1 | Aberta em rascunho, `a0b60a8a74d7b54af77ccf02b287359193401925`, base checkpoint; dois commits/24 caminhos | Não trocar base nem reaplicar os mesmos patches na consolidação. |
| Árvore original | HEAD checkpoint com alterações rastreadas e não rastreadas de vários módulos | É evolução posterior e parcialmente divergente da PR #1; não é um único patch publicável. |

A API pública retornou zero tags e zero releases. Na leitura inicial em sandbox, o provedor de credenciais do Windows falhou; na worktree isolada com permissão de rede, `gh auth status`, `push` e a abertura das PRs #2 e #3 funcionaram. As falhas iniciais eram de acesso do ambiente, não divergência do repositório. O dry-run integral de Release Please 17.11.2 contra a branch de Sistema terminou com código 0: proporia uma PR em rascunho `release 0.1.0`, sem escrita remota. Alguns bullets herdados do histórico aparecem em inglês, apesar dos títulos de seção em português; curá-los na primeira PR de release é pendência obrigatória. A PR automática real só poderá ser verificada depois do merge revisado do workflow em `main`.

A API pública também retornou 404 para `.github/workflows` em `main`; o workflow de Release Please está apenas na árvore local, não ativo no repositório publicado.

## Sobreposição com a PR #1

Dos 24 caminhos da PR #1, cinco tinham conteúdo idêntico na árvore original: `src/lib/pacienteCpf.ts`, as migrations `20260924120000` e `20260924130000`, `tests/operacional/pacientes-design.html` e `tests/operacional/pacientes-design.tsx`. Os 19 divergentes foram examinados como evolução posterior, não como conflito presumido. A PR #3 parte do commit da PR #1: aproveita o cadastro e Agenda já presentes, substitui somente a superfície própria de Pacientes, preserva os testes atuais e transfere dois props em `App.tsx`. O delta local de `Agenda.tsx` pertencia ao Prontuário e ficou fora. A PR #1 permanece em rascunho e na base checkpoint.

## Mudanças somente locais a separar

- **Sistema/versionamento:** página Sobre, logo Vencer Digital, créditos, notas, metadados de build, pacote/lockfile, configuração e workflow Release Please, testes e documentação. Esta tarefa prepara o diff local desses arquivos; não altera módulos clínicos.
- **Pacientes/Agenda além da PR #1:** foto privada, idade e responsável legal, listagem/resumo redesenhados, ajustes de formulário, testes e migrations `20260925100000`, `20260925120000`. Ambas constam como aplicadas nos checkpoints. A proposta `20260925130000` está em `supabase/review/`, não na fila de aplicação; conferir estado remoto e compatibilidade em etapa própria antes de decidir sua futura aplicação.
- **Login:** links de entrada por clínica, identidade e sessão, testes e documentação próprios. Separar da PR #1.
- **Prontuário e Financeiro:** alterações rastreadas locais adicionais e documentação/mocks preexistentes. Preservar e revisar em trilhas próprias; não incluí-las automaticamente na PR de Pacientes ou no pacote de versionamento.
- **Excluídos dos candidatos a commit:** `.env` e credenciais, dados reais, `scratch/`, `dist/`, capturas de sessão, ZIPs, caches npm e artefatos temporários. A lista de arquivos deve ser reapurada antes de cada PR; arquivos não rastreados não entram automaticamente.

## Recorte revisável de Sistema/versionamento

O inventário local contém 27 caminhos candidatos para uma PR **específica de Sistema/versionamento**:

- Metadados e automação: `.github/workflows/release-please.yml`, `.release-please-manifest.json`, `release-please-config.json`, `package.json`, `package-lock.json`, `scripts/check-release-notes.mjs`, `vite.config.ts`, `src/vite-env.d.ts`.
- Página e dados: `src/pages/SobreSistema.tsx`, `src/pages/sobre-sistema.css`, `src/config/instituicao.ts`, `src/config/notasEvolucao.json`, `src/config/notasEvolucao.ts`, `src/lib/papelApresentacao.ts`, `public/vencer-digital-logo.svg`.
- Navegação e teste: `src/App.tsx`, `src/components/shell/AppShell.tsx`, `src/components/shell/Sidebar.tsx`, `src/components/shell/types.ts`, `tests/operacional/sobre-sistema.spec.ts`, `tests/operacional/shell.tsx`.
- Documentação: `docs/00-LEIA-ME-IA.md`, `docs/VERSIONAMENTO-E-RELEASES.md`, `docs/modulos/sistema/00-README-SISTEMA.md`, `docs/modulos/sistema/01-DOCUMENTO-FUNCIONAL-MESTRE.md`, `docs/modulos/sistema/08-CHECKPOINT.md` e este inventário.

Na branch de Sistema, `src/App.tsx` recebeu somente a importação e renderização de `SobreSistema`; o restante do arquivo de Login e Prontuário da árvore original não foi copiado. `package.json` recebeu somente versão, verificação de notas e scripts pertinentes; o lockfile mudou apenas nos dois campos de versão. As notas embarcadas omitem o Login multi-clínica ainda fora da base. O módulo Sistema não usa os dados de clínica não confirmados presentes em outros arquivos locais. A revisão do staging da PR continua obrigatória antes do envio.

## Sequência de integração em revisão

1. Revisar a PR #2 (`main` como base): 37 commits/228 caminhos do checkpoint. O commit corretivo posterior restabelece os três artefatos históricos exatamente como em `d550211`; sem merge automático e sem execução de migrations.
2. Revisar a PR #1 ainda baseada no checkpoint; depois do merge da base, decidir expressamente sua nova base antes da integração em `main`.
3. Revisar a PR #3 contra a branch da PR #1: evolução posterior de Pacientes/Agenda sem duplicar as duas migrations de CPF. A trava `20260925130000` está apenas em `supabase/review/`, não aplicada nem enfileirada; exigir ordem de publicação e homologação próprias.
4. Revisar a PR #4 de Sistema/versionamento somente depois das dependências, com notas que descrevam seu código real. A combinação local das quatro PRs deve preservar ancestralidade: incorporar por merge local os commits corretivos da PR #3 à branch #4; após revisão, atualizar as bases de #1 para #2/main conforme a integração efetiva, #3 para #1 e #4 para #3, sem reaplicar patches. Login multi-clínica e deltas locais de Prontuário/Financeiro permanecem em trilhas separadas, fora dessas PRs. A árvore original permanece intocada.
5. Consolidar em `main` somente os recortes aprovados. A versão `0.1.0` continua em desenvolvimento até release comprovada; módulos pendentes não são anunciados como completos.
6. Com o workflow revisado e presente em `main`, verificar a primeira PR automática de release **em rascunho**: número exatamente `0.1.0`, changelog em português, manifesto/pacote/lockfile coerentes e notas da aplicação curadas. Só depois decidir merge da PR de versão, tag/release e publicação do frontend em etapas distintas.

`main` é o destino recomendado porque é a branch padrão confirmada e o ponto natural de integração; o checkpoint é uma linha histórica de trabalho, não uma arquitetura de lançamento aprovada. A worktree isolada de integração não substituiu a pasta original nem seu localhost.

## Combinação corretiva local

A worktree descartável `integracao-010-combinada/CLINICA PATRICIA`, branch local `codex/integracao-010-combinada-review`, foi criada da PR #4. Recebeu merges locais `--no-ff` da branch #2 e das correções subsequentes da #4. Assim os commits de #2, #1, #3 e #4 são ancestrais, sem cherry-pick, rebase ou reaplicação de patches. O código combinado foi validado em `1e5d1d71b77c7c616f1beeee46333c536be28cd3`. O teste repetido de cadastro revelou uma corrida real de cursor; a correção está na PR #3 e o teste determinístico na #4. A nova tarefa de ordenação/filtros da árvore original não foi incorporada.

Ordem futura, sujeita a aprovação: merge commit da #2 em `main`; então retarget da #1 para `main` e merge commit; depois retarget da #3 para `main` e merge commit; por fim retarget da #4 para `main` e merge commit. Rever o diff e os checks a cada retarget. Evitar squash/rebase nessa cadeia, pois descartariam a ancestralidade usada pelas PRs dependentes e poderiam reapresentar mudanças. Nenhuma dessas alterações de base ou merges remotos foi executada nesta revisão.

Os três blobs históricos restaurados coincidem com `d550211`; o delta combinado contra esse checkpoint passa em `git diff --check`. A proposta de trava está somente em `supabase/review`; as migrations estruturais de responsável e foto não mudaram. Nenhum arquivo de credenciais, dados reais, scratch, dist ou captura foi adicionado pelos commits corretivos.

Resultado da combinação: instalação por `npm ci` raiz/servidor; notas 4/4, Pacientes 13/13, Financeiro 15/15, servidor 10/10 com variáveis sintéticas, typecheck/build frontend e servidor e lint com saída 0. Após corrigir a corrida de cursor descoberta na primeira execução, a suíte integral com `CI=true` e Chromium encerrou com **47 aprovados, quatro skips deliberados e código 0**; a regressão de cadastro também passou 9/9 em repetições. Execução local no Windows usando comandos portáveis e o navegador escolhido pelo CI, não uma execução Linux: o job Ubuntu precisa ser conferido no GitHub. Permanecem dois avisos de lint, avisos de bundle e o audit do servidor (uma vulnerabilidade moderada e uma alta), sem atualização automática de dependências fora do escopo. Nenhum teste autenticado real foi repetido; recepção, médico e vínculo de clínica única continuam pendentes.
