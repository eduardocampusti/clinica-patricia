# CHECKPOINT — PACIENTES

## Estado em 25/09/2026

- Projeto Supabase confirmado: `Clinica Patrícia` (`xftnkusbyqzyvzrovroj`).
- Migration `20260924120000_pacientes_cpf_pendente_rpc.sql`: aplicada anteriormente.
- Migration `20260924130000_pacientes_busca_cpf_segura.sql`: aplicada de forma controlada com `supabase migration up --linked`.
- Histórico local e remoto alinhado até `20260924130000` após a aplicação.
- Migrations `20260925100000_pacientes_responsavel_legal.sql` e `20260925120000_pacientes_foto_privada.sql`: aplicadas separadamente em 25/09/2026 ao projeto vinculado, após ensaios SQL com `ROLLBACK`. Histórico local/remoto alinhado até `20260925120000`.
- RPC `public.paciente_buscar_por_cpf(uuid, text)` confirmada como `SECURITY DEFINER`, `STABLE` e com `search_path=pg_catalog, public`.
- Execução da nova RPC permitida a `authenticated` e negada a `anon`.
- A resposta da RPC contém somente `id`, `nome_completo`, `data_nascimento`, `telefone`, `endereco` e `ativo`; não retorna CPF integral, ciphertext ou hash.
- `cpf_decrypt(bytea)` e seus privilégios anteriores foram preservados para compatibilidade: `authenticated` e `service_role` continuam permitidos; `anon` continua negado.
- Smoke transacional com dados sintéticos passou para proprietária, recepção, médico negado, CPF inválido, paciente inativo, duas clínicas com o mesmo CPF, clínica inativa e contexto divergente.
- O smoke terminou com `ROLLBACK`; verificação posterior confirmou ausência dos usuários, clínicas e pacientes sintéticos.

## Versão local Stitch e Agenda

- Branch local de revisão: `codex/pacientes-stitch-agenda`, baseada em `codex/checkpoint-local-2026-08-14` no commit `d550211886ad93ad0c58d49f540aac8f81a19536`.
- O cadastro local usa o desenho Stitch como referência visual, mas apresenta somente campos, estados e integrações realmente suportados.
- Etapas indisponíveis não são apresentadas como concluídas; convênio, validação na Receita, consentimento genérico e emissão automática de prontuário não são simulados como funcionalidades ativas.
- O contato do paciente é apresentado como “Telefone / WhatsApp”, sem indicar verificação do número ou ativar mensagens automáticas. O avatar ilustrativo acompanha as iniciais digitadas e usa uma silhueta neutra sem nome.
- A formatação de nome, logradouro, bairro e cidade ocorre durante digitação, colagem e substituição, preserva composição/cursor/seleção e permite correção interna de grafia excepcional sem desativar a formatação do próximo conteúdo.
- A interface local oferece foto opcional por arquivo ou webcam, prévia, confirmação, troca e remoção, além de uma ação administrativa posterior. A migration `20260925120000` instalou bucket privado (5 MB; JPEG/PNG/WebP), três policies e três RPCs. Ensaios SQL sintéticos e um fluxo autenticado real de proprietária em Ipupiara passaram para upload, download após reabrir a página, troca e remoção da imagem sintética. O frontend não foi publicado.
- O fluxo “Novo paciente” da Agenda preserva clínica, profissional, data, horário e observações. Cancelar retorna sem criar paciente; salvar retorna com o paciente da mesma clínica selecionado.
- A troca de clínica suspende superfícies sensíveis, associa respostas à clínica que originou a carga e limpa o formulário de paciente em transição.
- Capturas sintéticas de identificação e endereço foram geradas para desktop, tablet e celular e permanecem fora dos arquivos candidatos à PR.
- A suíte operacional passou em 27 cenários e inclui regressão que impede `INSERT` quando o e-mail não satisfaz a validação nativa do formulário. Os testes unitários de preenchimento permanecem em 7 cenários; build e verificação de tipos passaram. O lint mantém somente advertências preexistentes fora do módulo.

## Lacuna obrigatória antes do fluxo completo de menores

- A regra aprovada determina que todo cadastro concluído de paciente menor possua ao menos um responsável legal vinculado na mesma clínica.
- A árvore original possui formulário condicional de responsável e chamada à RPC atômica `paciente_menor_criar_com_responsavel`. A migration estrutural `20260925100000` está aplicada; o ensaio SQL pós-aplicação confirmou gravação atômica, campos obrigatórios, CPF opcional, proprietária/recepção autorizadas, médico negado e isolamento de duas clínicas sintéticas, tudo com `ROLLBACK`. Um cadastro sintético via frontend com sessão real de proprietária em Ipupiara confirmou paciente menor e um responsável na mesma clínica, CPF ausente e foto privada. Isso não comprova sessões reais de recepção ou médico. A trava contra `INSERT` direto de menor sem responsável segue pendente; portanto, o fluxo completo ainda não está concluído.
- A lacuna não inventa responsável e não cria bloqueio de agendamento ou atendimento. O tratamento operacional de menores já existentes e de data de nascimento ausente continua dependendo das decisões registradas no Documento Funcional Mestre.
- A idade é calculada no navegador, sem coluna persistida. No cadastro novo, data ausente, inválida ou futura interrompe a conclusão com explicação, sem criar exceção para menor conhecido; isso não bloqueia agendamento ou atendimento de pacientes existentes. A obrigatoriedade funcional definitiva da data ainda precisa de decisão do proprietário.
- A migration de trava `20260925101000` da worktree de proposta apresentou uma incompatibilidade concreta: os triggers consultam a tabela privada sem privilégios de leitura. O ensaio corrigido com `SECURITY DEFINER` passou em `ROLLBACK`; a versão corrigida está em `supabase/review/` e **não** em `supabase/migrations/`. A trava não será aplicada antes de frontend compatível em uso, avaliação de fluxos legados e homologação autenticada. Um ensaio transacional após as duas migrations aplicadas confirmou que, sem a trava, uma chamada direta da recepção ainda consegue inserir menor com nascimento conhecido sem responsável. O legado remoto possuía zero menores com nascimento conhecido e um cadastro sem data no momento da consulta agregada; nenhum dado identificável foi lido.
- A prévia de foto permanece no estado do cadastro entre Identificação e Endereço; trocar de clínica ou cancelar desmonta o editor e libera a câmera/URLs temporárias. Upload ocorre apenas ao salvar após confirmação da foto. O ensaio SQL da migration de foto passou em `ROLLBACK` para papéis/contextos simulados. Na sessão real de proprietária, a foto sintética permaneceu visível após reabrir a aplicação; a troca substituiu o objeto sem deixar a versão anterior, e “Remover foto” desvinculou e apagou o objeto. A verificação final encontrou zero objetos sob o prefixo do teste.
- Rechecagem após aplicação: `supabase migration list --linked` alinha o histórico local/remoto até `20260925120000`. Os 11 testes unitários de formulário/foto e os 2 de idade passaram; os 6 cenários operacionais dirigidos de foto e responsável passaram em desktop/tablet/celular. O runner Playwright permaneceu aberto após anunciar 6/6 e foi encerrado manualmente, sem saída final de sucesso. A prévia local para homologação autenticada está em `http://127.0.0.1:4195/acesso/brotas`; nenhuma sessão real de teste foi utilizada nesta rechecagem.
- A versão corrigida da trava foi ensaiada novamente sobre o schema remoto já instalado, somente dentro de `BEGIN`/`ROLLBACK`: a RPC atômica concluiu com um responsável, enquanto `INSERT` direto de menor sem responsável e exclusão do único responsável receberam violação de regra. A exclusão do próprio paciente em ensaio removeu o vínculo em cascata. A verificação posterior encontrou zero registros/clínicas sintéticos e zero triggers persistidos. A contagem agregada do legado continua em zero menores com nascimento conhecido e um paciente sem data. A trava não foi aplicada. O bucket privado tinha zero objetos antes da tentativa de homologação autenticada.
- Na homologação autenticada, a clínica ativa era Ipupiara. Ao trocar para Brotas, a interface mostrou estado de carregamento e a busca pelo marcador do paciente sintético de Ipupiara não retornou resultado; ao voltar, o paciente reapareceu. Uma conta proprietária vinculada às duas clínicas não demonstra o bloqueio para usuário vinculado somente a uma delas. A separação por papel em sessão real de recepção/médico ainda depende de contas de teste autorizadas; as negativas desses papéis foram verificadas apenas em ensaios SQL com identidade simulada.
- O paciente sintético tinha zero referências em Agenda, Atendimentos, Lista de Espera e Financeiro. Após remover a foto pela interface, a exclusão transacional por ID e clínica exatos removeu o paciente e seu responsável em cascata. Consulta final: zero pacientes, zero responsáveis e zero objetos Storage sob o prefixo exclusivo do teste. A tentativa inicial de limpeza pelo CLI retornou `deleted: []` e não removeu o objeto; a remoção efetiva foi pelo frontend, confirmada no banco. Nenhum dado real foi alterado.
- Continuação da trava: o histórico vinculado confirmou `20260925120000` como última migration remota. Foi preparada `supabase/migrations/20260925130000_pacientes_menor_exigir_responsavel.sql`, sem mover o rascunho histórico `20260925101000` de `supabase/review/` e sem aplicar a nova migration. A proposta confere menores legados conhecidos sem responsável antes da instalação, avalia o estado final da linha em trigger diferido e serializa remoções concorrentes do vínculo. No projeto vinculado, o ensaio `scratch/pacientes-trava-20260925130000-rollback-test.sql` passou integralmente em uma transação com `ROLLBACK`: a RPC criou menor e responsável; INSERT direto de menor conhecido sem responsável e exclusão do único vínculo foram rejeitados. O texto da própria migration, retirando apenas comentários e substituindo `COMMIT` por `ROLLBACK` em memória, também passou no projeto vinculado. Consulta independente depois encontrou zero usuários Auth, usuários locais, vínculos, clínicas, pacientes, responsáveis, fotos, funções e triggers sintéticos persistidos. O histórico remoto continua sem `20260925130000`.
- A consulta agregada do legado confirmou zero menores com nascimento conhecido e um cadastro sem data de nascimento; o registro não foi aberto nem modificado. Idade não pode ser inferida desse cadastro. A proposta técnica é manter explicitamente “idade não determinada”, sem classificação automática como adulto e sem responsável fictício; o fluxo regular novo permanece impedido de concluir sem data na interface local atual, mas uma chamada direta com data nula continua fora da trava proposta. Cabe ao proprietário decidir o procedimento excepcional para nascimento desconhecido, inclusive identificação, responsável quando a menoridade for suspeita e continuidade de atendimento sem transformar a pendência em bloqueio clínico.
- Não foram encontradas contas de teste autorizadas nem credencial administrativa com ciclo verificável de criação/remoção de usuários Auth temporários. Portanto, nesta continuação **não houve** homologação de sessões reais de recepção, médico ou pessoa vinculada a apenas uma clínica. Resultados SQL com papel/identidade simulados de ensaios anteriores não foram promovidos a testes autenticados. A sessão real de proprietária com acesso às duas clínicas, concluída anteriormente, não prova a negativa entre clínicas para vínculo único.
- A escolha entre entidade própria de responsáveis, vínculo com pessoa já cadastrada ou outro modelo permanece para o desenho técnico. Não há identidade global nem compartilhamento automático entre Brotas e Ipupiara.

- Ajuste local do modal: Identificação do adulto utiliza a largura ao lado da foto, com cabeçalho, aviso e espaçamentos compactados; o responsável do menor fica em etapa própria. Apenas etapas disponíveis são exibidas. Em teste sintético, adulto e responsável couberam sem rolagem interna em 1440×1000 e 1366×768; o adulto precisou rolar em 1366×600 e no celular 390×844, enquanto tablet 820×1180 não precisou. No celular, o responsável também precisou rolar, com campos alcançáveis e rodapé visível. A mudança não altera validação, persistência, migrations ou autorização. As capturas sintéticas e os testes responsivos estão em `scratch/pacientes-layout-*.png` e `tests/operacional/pacientes-layout.spec.ts`.
- Revisão visual de Endereço e Contatos: em cenário sintético com CEP preenchido, a área interna teve 101 px de rolagem em 1366×768, nenhuma em 1440×1000 e tablet 820×1180, e 659 px no celular 390×844 (671 px no fluxo de menor devido ao indicador de três etapas). A compactação foi restrita à etapa Endereço no desktop; campos, aviso de privacidade e rolagem permanecem. Capturas com CEP e validação em `scratch/pacientes-endereco-*.png`; o teste `tests/operacional/pacientes-endereco-layout.spec.ts` confirma retorno entre etapas, foto, dados e rodapé, sem consulta real ao ViaCEP ou ao banco. O runner Playwright gerenciando o Vite via `webServer` não encerrava no Windows depois dos testes; `npm run test:operacional` agora controla o ciclo do Vite diretamente e a configuração Playwright admite servidor externo. A suíte operacional sintética encerrou com 39/39 e código 0 nesse modo. Nenhuma operação no Supabase nem aplicação da migration `20260925130000` ocorreu nesta revisão.
- Ajuste final da mesma etapa: em 1366×768, Observações (duas linhas redimensionáveis) e o aviso informativo ficam lado a lado; os contatos deixam de ter cartões internos no desktop. A rolagem normal caiu de 101 px para 0 px sem reduzir fontes nem a altura mínima dos inputs. Em 1366×600 e no celular, a rolagem continua; o teste verifica que Observações pode ficar integralmente acima do rodapé, inclusive com mensagem de CEP não encontrado. Identificação, Responsável legal, campos e regras de salvamento não mudaram. As capturas sintéticas `scratch/pacientes-endereco-*.png` foram atualizadas.
- Navegação visual local: “Convênios — Em planejamento” foi recolocado entre Identificação e Endereço e Contatos como indicação não interativa, sem número, marca de conclusão, formulário ou dados persistidos. As etapas disponíveis mantêm a numeração anterior (adulto: 1 e 2; menor: 1, 2 e 3 com Responsável legal). O conteúdo e as validações permanecem inalterados; não há elegibilidade TISS ou verificação de convênio implementada.
- Redesenho local da página principal: cabeçalho com clínica atual, alternância Nome/CPF exato, lista de pacientes ativos com nascimento/idade calculada, contato formatado, status e foto privada ou iniciais; o resumo selecionado consulta responsável legal e pendência do CPF, mostra o endereço textual literalmente e reutiliza inclusão de CPF e gestão de foto. A navegação “Ir para Agenda” não pré-seleciona paciente. O resumo móvel é sobreposto e a troca de clínica invalida seleção, consultas e respostas antigas. A busca por nome permanece local e sem paginação; nenhum dado fictício foi inserido na interface real. O frontend não foi publicado nem houve alteração de migration.
- Verificação do redesenho: 11 testes unitários de Pacientes e 42 cenários operacionais sintéticos passaram com saída 0 em desktop, tablet e celular, incluindo busca exata, resumo, teclado e troca de clínica com latência. Typecheck, lint, build e `git diff --check` passaram; o lint mantém um aviso preexistente em `ThemeProvider.tsx` e o build informa chunks grandes. Uma disputa entre edição e reposição do cursor do nome apareceu na primeira execução móvel, foi corrigida sem alterar o visual do modal, passou 3/3 em repetição isolada e 42/42 na suíte final. Capturas sintéticas da página estão em `scratch/pacientes-lista-{desktop,tablet,mobile}.png`. Não houve homologação autenticada nova nesta etapa.
- Acabamento visual posterior da página principal: cabeçalho, busca integrada, lista e resumo foram recompostos com superfícies claras, ícones existentes do projeto e o azul `#006194` já usado no modal, sem mudar o menu global ou o cadastro. O cabeçalho “Ação” foi alinhado aos links “Ver resumo”. Após a retirada de um CTA duplicado no estado vazio, a suíte operacional integral terminou com 45/45 e código 0; 11 testes unitários, build/typecheck, lint e `git diff --check` também passaram. O lint conservou apenas o aviso preexistente em `ThemeProvider.tsx`; o build conservou avisos de chunks grandes. Capturas sintéticas da página completa: `scratch/pacientes-pagina-{desktop,tablet,mobile}.png` e variantes `-resumo`. A sessão autenticada de Brotas mostrou a nova composição, o resumo abriu sem gravação, e a cor computada dos controles foi `rgb(0, 97, 148)`. O frontend não foi publicado e nenhum paciente real foi alterado.

## Pendente

- Publicar o frontend que utiliza a busca exata e deixa de descriptografar CPFs em massa na listagem.
- Homologar a chamada pelo frontend publicado em Brotas e Ipupiara com contas de teste autorizadas, sem alterar pacientes reais.
- Revogar futuramente a execução direta de `cpf_decrypt` para clientes autenticados somente depois de confirmar que nenhuma versão publicada ou consumidor autorizado ainda depende dela.
- Revisar a PR isolada antes de qualquer merge ou publicação.
- Homologar com contas reais de teste de recepção e médico (ou outra forma autenticada autorizada) para não confundir ensaios SQL simulados com limites de sessão reais; conferir usuário vinculado apenas a uma das clínicas. O fluxo de proprietária em Ipupiara foi homologado e o dado sintético foi integralmente removido.
- Publicar e homologar frontend compatível antes de considerar a nova trava `20260925130000`; rever os consumidores legados e a decisão pendente sobre nascimento ausente. O arquivo `20260925101000` em `supabase/review/` permanece apenas como histórico da proposta anterior.
